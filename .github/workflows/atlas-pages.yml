name: FLOW Atlas — GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'app/**'
      - 'scripts/**'
      - 'modeles/**'
      - 'Lancer-FLOW-Atlas.ps1'
      - 'requirements.txt'
      - '.github/workflows/atlas-pages.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: atlas-pages
  cancel-in-progress: false

jobs:
  launcher:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: '3.14'
      - run: python -m unittest discover -s app -p test_launcher.py
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          # Retired snapshots are read at the exact commits in git-history.json.
          fetch-depth: 0
      - uses: actions/setup-python@v7
        with:
          python-version: '3.14'
      - uses: actions/setup-node@v7
        with:
          node-version: '26'
      - uses: pnpm/action-setup@v6
        with:
          version: 11.19.0
      - run: python -m pip install -r requirements.txt
      - run: pnpm --dir app install --frozen-lockfile
      - run: pnpm --dir app test
      - run: python -m unittest discover -s scripts -p "test_*.py"
      - run: python -m unittest discover -s app -p "test_*.py"
      - run: pnpm --dir app build
      - run: pnpm --dir app exec playwright install --with-deps chromium
      - run: pnpm --dir app verify
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v4
        with:
          path: app/dist
  deploy:
    needs: [build, launcher]
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy Atlas
        id: deployment
        uses: actions/deploy-pages@v4
